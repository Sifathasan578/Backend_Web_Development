Table of Contents: 

What is collections framework 
Core interfaces in the collections framework 
How it simplifies data handling 
Traversing collections 
Aggregate operations 
For each construct 
Iterators 
Collections interface bulk operations 
List interface 
Common implementation classes of the list interface 
ArrayList 
ArrayList vs LinkedList 
Set interface 
Hashset
LinkedHashSet 
TreeSet 
Map interface 
HashMap 
LinkedHashMap 
TreeMap 
Choosing the right collection in java 
When to use map vs set 

Collection Framework:
interfaces provide kore
interface contract rakhe then pore implement kore 
jemon list interface, arraylist implementation of list 
collection: object represents group of objects 
collection framework: set of classes and interfaces … store, manipulate, and access groups of objects efficiently  

Core interfaces: List, Set, Map
Core classes: ArrayList, HashSet, HashMap, LinkedList 

set = unique elements thake
map = dictionary = key, value pair

list kono data structure er moddhe pore? 
dynamic array. size unmentioned. like vectors. 

Benefits: 
reduce programming effort 
increases performance 
provides interoperability 
reduces the effort required to learn APIs 
reduces the effort required to design and implement APIs
foster software reuse 

Traversing collections: 
Aggregate operations (Streams)
after jdk 8
use streams + lambda expressions 
non-mutative 
… parallelized for performance 
example for later 
For each construct 
easy to read 
cannot remove elements inside loop 
Iterators 
step through elements one by one 
methods: 
hasNext()
next() 
remove()

for(Iterable<String> it = list.iterator(); it.hasNext();) {
            if(it.next().isEmpty()) {
                it.remove(); // safe removal 
            }
        }

shob shomoy check kore porer ghorer jonno. ejonno safe case. 
iterator rarely kaze lage

collection interface bulk operations: 
containall
addAll 
removeAll
…  … 

List interface: 
ordered 
duplicates allowed 
indexed access 
dynamic size 
ArrayList, LinkedList, Stack, Vector 
ArrayList: 
implements list, extends collection 
…
…

class implements interface 
interface extends interface 

ArrayList: 

collections er vitore primitive use kora jay nah 
add(1, element): ektu costly
add(index), .remove(ele); easy O(1), last e boshay 


.remove(ele); costly 
.add(null) 


string pool: recap 

LinkedList: 
doubly linked list … back pointer + node + next pointer
last element null k point kore 
nodes: data, reference to the next node  

What benefit to use ArrayList instead of LinkedList? 
index er through te access kora easier for ArrayList. linkedlist er khetre prothom theke traverse korte hobe! 

Benefit of LinkedList: 
3 size er ekta array te arekta element dhukaite chaile array size ta double hoy kintu linkedlist er khetre link gula delete kore notun link kore dey. khub faster. 
Insertion and Deletion er khetre khub kazey dey 

Set: 
Duplicates rakhe nah 
searching er khetre onek valo
no guaranteed order of insertion 
one null allowed 
set hashtable data structure use kore 
set implements: enumset, hashset, linkedhashset, treeset 
an element exist or not can be achieved in O(1) 
efficient search, insertion, deletion operations 



HashSet: 
implements set interface 
does not maintain order
duplicate allow nah
one null allowed 
uses hashMap internally 
not thread safe, multiple threads accept korte gele exception khabe. 
set hashtable use kore, oita hocche ekta array 

LinkedHashSet: 
Maintains the order of insertion
stores unique
faster iteration compared to hashset 
allows null elements 

TreeSet: 
implements NavigableSet interface 
does not allow duplicates, null values … 
Stores unique elements in sorted order. Naturally or by using comparator 
eita kore balanced binary search tree use kore kore or red black tree use kore kore 

Map Interface 
key value pair 
keys are unique elements, values can be duplicates 
in python it is called dictionaries 
one null accepts 
for thread safety there are features 

HashMap: 
implements map 
… … 
uses hashing internally for fast access, insertion, and deletion 
allows one null key 
insertion order maintain kore nah
	
LinkedHashMap: 
stores unique key value pair 
maintains insertion order
allows one null key

TreeMap: 
sort kore key dhore 
custom comparator use kore sorting ta kora jabey 
implements map and navigableMap 
no null keys allowed 
allows multiple null values 
navigation methods: higher, lower, ceiling, floor 

Do further practice with collections 

Choosing right collection in java

